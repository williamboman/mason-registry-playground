---
name: Package tests

on:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: mason-org/actions/registry-lint@testing

  test:
    needs: lint
    uses: mason-org/actions/.github/workflows/package-tests.yaml@testing
